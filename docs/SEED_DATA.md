# 数据填充脚本使用指南

## 📦 功能说明

`cmd/seed/main.go` 是用于快速填充测试数据的Go程序，包含：
- 4个测试用户（1个admin + 3个普通用户）
- 20个挑战题目（覆盖5个分类，3个难度级别）

---

## 🚀 使用方法

### 1. 运行种子数据脚本

```bash
# 在项目根目录执行
go run cmd/seed/main.go
```

### 2. 预期输出

```
【1/3】清空旧数据...
✓ 旧数据已清除

【2/3】插入测试用户...
✓ 已插入 4 个测试用户

【3/3】插入挑战题目...
✓ 已插入 20 个挑战题目

==================================================
📊 数据统计
==================================================

分类分布：
  Web: 8 个题目
  Pwn: 4 个题目
  Crypto: 4 个题目
  Reverse: 3 个题目

难度分布：
  Easy: 5 个题目
  Medium: 7 个题目
  Hard: 8 个题目

总分值：6070 分
==================================================

🎉 数据填充完成！
```

---

## 📋 数据清单

### 测试用户（4个）

| 用户ID | 用户名 | 角色 | 邮箱 |
|:-------|:-------|:-----|:-----|
| admin | admin | admin | admin@cyber-range.com |
| user_alice | alice | user | alice@example.com |
| user_bob | bob | user | bob@example.com |
| user_carol | carol | user | carol@example.com |

### 挑战题目（20个）

#### Web 分类（8个）
- **Easy（3个）：** Nginx配置泄露、Apache目录遍历、Flask调试模式
- **Medium（3个）：** SQL注入、XSS、SSRF
- **Hard（2个）：** PHP反序列化、JWT加密算法混淆

#### Pwn 分类（4个）
- **Medium（2个）：** SUID提权、栈溢出入门
- **Hard（2个）：** ROP链构造、Linux内核提权

#### Crypto 分类（4个）
- **Easy（1个）：** Base64多重编码
- **Medium（1个）：** 凯撒密码变种
- **Hard（2个）：** RSA低加密指数、AES ECB模式

#### Reverse 分类（3个）
- **Easy（1个）：** 字符串隐写
- **Medium（1个）：** UPX加壳程序
- **Hard（1个）：** 代码混淆与反混淆

---

## ⚙️ 自定义数据

### 添加新题目

编辑 `cmd/seed/main.go` 中的 `getChallenges()` 函数：

```go
{
    ID:          "your-challenge-id",
    Title:       "你的题目标题",
    Description: "题目描述",
    Category:    "Web",  // Web/Pwn/Crypto/Reverse
    Difficulty:  "Easy", // Easy/Medium/Hard
    Image:       "nginx:alpine",
    Flag:        "flag{your_flag_here}",
    Points:      100,
    CreatedAt:   now,
    UpdatedAt:   now,
},
```

### 添加新用户

编辑 `cmd/seed/main.go` 中的 `getTestUsers()` 函数。

---

## ⚠️ 注意事项

### 1. 数据会被清空
脚本会先清空所有现有数据，包括：
- ✅ challenges（挑战题目）
- ✅ instances（运行中的实例）
- ✅ submissions（提交记录）
- ✅ users（用户数据）

**⚠️ 生产环境请勿运行！**

### 2. 密码Hash
当前用户的 `password_hash` 使用的是假数据 `$2a$10$dummyhash`。

真实环境需要使用bcrypt生成：
```go
import "golang.org/x/crypto/bcrypt"

hash, _ := bcrypt.GenerateFromPassword([]byte("password"), 10)
```

### 3. Docker镜像
某些题目使用的Docker镜像可能需要提前拉取：
```bash
docker pull nginx:alpine
docker pull python:3.9-slim
docker pull ubuntu:20.04
docker pull vulnerables/web-dvwa
docker pull php:7.4-apache
docker pull node:16-alpine
```

---

## 🔄 与测试脚本配合

种子数据脚本生成的题目ID不会与 `test_core_features.sh` 冲突：
- 测试脚本使用：`test-1`, `test-2`, ...
- 种子数据使用：`web-nginx-001`, `pwn-suid-001`, ...

---

## 🎯 快速开始流程

```bash
# 1. 填充数据
go run cmd/seed/main.go

# 2. 启动后端
go run cmd/api/main.go

# 3. 启动前端
cd web && npm run dev

# 4. 访问浏览器
# http://localhost:5173
# 现在可以看到20个真实的CTF题目！
```

---

## 📝 编译为独立程序（可选）

```bash
# 编译
go build -o seed cmd/seed/main.go

# 执行
./seed
```

这样可以分发给团队成员快速初始化数据库。
